import React, { useState } from 'react';
import { View, Text } from 'react-native';

const Login = () => {
  const [login, setLogin] = useState();

  return (
    <View>
      <Text />
    </View>
  );
};
